version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: checkout
          commands:
            - checkout
        - name: python -m pip install --upgrade pip
          commands:
            - python -m pip install --upgrade pip
        - name: install flake8
          commands:
            - python -m pip install flake8==2.5.0
after_pipeline:
  task:
    jobs:
      - name: run flake8
        commands:
          - python -m flake8 .
