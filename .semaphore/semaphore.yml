version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: checkout
          commands:
            - checkout
        - name: sem-version python 3.8
          commands:
            - sem-version python 3.8
        - name: python -m pip install --upgrade pip
          commands:
            - python -m pip install --upgrade pip
        - name: 'pip install -e .[dev]'
          commands: []
after_pipeline:
  task:
    jobs:
      - name: run flake8
        commands:
          - flake8 .
